import { PetRacePerkDescriptionPipe } from './pet-race-perk-description.pipe';

describe('PetRacePerkDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new PetRacePerkDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
